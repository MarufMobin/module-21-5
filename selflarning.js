// Three types of Variables

const yourName = "Maruf";
const yourAge = 22;
const yourMarridStatus = false;


// Create two variable and undestand to why using this also change a value

const yourFatherName = "Md Fazlul Haque";
const yourMotherName = "Ms Monowara Begum";
let yourAddress = "Dhaka";
yourAddress = "Mirpur";


// Define two variable and addition / multipication / division / minus
// Addition
let shirtPrice = 500;
let pantPrice = 400;

let totalPriceOfItems = shirtPrice + pantPrice;

// Minus
const myPocketBalence = 1000;
let parchesGift = 300;

let restMoney = myPocketBalence - parchesGift;

// Division

const mangoes = 40;
let pepoles = 8;

let dividedMangoes = mangoes / pepoles;

// Multiplication
const myGoats = 10;
let everyOneGoatsBaby = 2;

let myTotalGoats = myGoats * everyOneGoatsBaby;


// Using six types of Oparator and create Oparation

// let fistNumber = 50;
// let secondNumber = 30;

// using smaller then sign

/* if( fistNumber < secondNumber ){
    console.log( fistNumber, "is Smaller then",secondNumber );
}else{
    console.log( secondNumber , "is Smaller then", fistNumber );
} */

// using grater then sign

// let thirdNumber = 60;
// let fourthNumber = 60;

/*     if( thirdNumber > fourthNumber ){
        console.log( thirdNumber,"is Gratter then" ,fourthNumber );
    }else{
        console.log( fourthNumber , "is Grater then", thirdNumber );
    }
 */

// Both Values Are not Equal Or Equal Define and using == sign

   /*  if( thirdNumber == fourthNumber ){
        console.log( "Both Values are Equal" );
    }else{
        console.log( "Both Values are Not Equal" );
    } */

// using != sing to find out value are equal or not equal

   /*  let fistNumber = 50;
    let secondNumber = 50;

    if( fistNumber != secondNumber ){
        console.log("Fist Number and second Number are Not Equal");
    }
    else{
        console.log("Fist And Second Number are Equal");
    } */

// Using <= oparator to find out Smallest and equal Value

   /*  let fistNumber = 50;
    let secondNumber = 80;
    
    if( fistNumber <= secondNumber ){
        console.log( fistNumber, "is Smaller Then" , secondNumber)
    }else{
        console.log( secondNumber, "is Smaller Then" , fistNumber)
    } */

// Using >= to find out Larges value in two numbers

  /*   let fistNumber = 150;
    let secondNumber = 80;

    if( fistNumber >= secondNumber ){
        console.log(fistNumber,"is Bigger then", secondNumber);
    }else{
        console.log(secondNumber,"is Bigger then", fistNumber);
    } */

// using && sing to Varified PassWord

   /*  let userName = "Maruf";
    let password = 123;

    if( ( userName == "Maruf" ) && ( password == 123 ) ){
        console.log("WelCome User");
    }else{
        console.log("Wrong Access");
    } */

// Using || Oparator to find out one condition is True then is it Ok

    let writingBook = false;
    let readingBook = false;

    if( writingBook || readingBook ){
        console.log("Goto Out site for tour");
    }else{
        console.log("Don't Goto Out fide in this Room");
    }



